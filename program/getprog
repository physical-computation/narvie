#!/bin/sh

#./objdump | grep ".*:" | sed "s/.*:\t//g" | grep ".*       " | sed "s/ .*//g" > program.hex

srec_decoder/srec_decode led.sr

cp program.hex ../submodule/verilog/program.hex
cp data.hex ../submodule/verilog/data.hex
